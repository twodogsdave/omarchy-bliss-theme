$background = rgba(204, 204, 204, 0.10)
$shadow_color = rgba(00000055)
$shadow_inactive = rgba(00000044)
$active_border = rgba(47474766) rgba(33333322) 90deg
$inactive_border = rgba(33333360)

$group_border_active = rgba(47474755) rgba(33333322) 90deg
$group_border_inactive = rgba(33333355)
$group_locked_active = rgba(47474788) rgba(33333322) 90deg
$group_locked_inactive = rgba(33333377)
# $groupbar_active = rgba(CCCCCC0F)
# $groupbar_inactive = rgba(33333333)
# $groupbar_locked_active = rgba(47474733)
# $groupbar_locked_inactive = rgba(33333355)
# $text_active = rgba(474747dd)
# $text_inactive = rgba(33333399)


general {
    gaps_in = 4
    gaps_out = 8
    border_size = 2
    col.active_border = $active_border
    col.inactive_border = $inactive_border
    resize_on_border = true
    extend_border_grab_area = 15
    allow_tearing = false
    layout = dwindle
}

group {
    col.border_active           = $group_border_active
    col.border_inactive         = $group_border_inactive
    col.border_locked_active    = $group_locked_active
    col.border_locked_inactive  = $group_locked_inactive
}

group:groupbar {
    enabled = true
    font_size = 10
    gaps_in = 3
    gaps_out = 3

    # text_color = $text_active
    # text_color_inactive = $text_inactive
    # col.active = $groupbar_active
    # col.inactive = $groupbar_inactive
    # col.locked_active = $groupbar_locked_active
    # col.locked_inactive = $groupbar_locked_inactive
}

animations {
    enabled = true

    bezier = water, 0.22, 0.9, 0.36, 1.0
    bezier = flow, 0.25, 0.1, 0.25, 1.0       
    bezier = ripple, 0.33, 0.0, 0.2, 1.0
    bezier = stream, 0.4, 0.0, 0.4, 1.0       
    bezier = cascade, 0.19, 1.0, 0.22, 1.0

    animation = windows,    1, 3.0, water           
    animation = windowsIn,  1, 2.8, cascade
    animation = windowsOut, 1, 2.4, stream
    animation = windowsMove,1, 1.6, flow             
    animation = fade,       1, 2.4, water
    animation = fadeIn,     1, 2.0, cascade
    animation = fadeOut,    1, 1.8, ripple
    animation = fadeDim,    1, 2.0, water
    animation = fadeSwitch, 1, 1.4, flow              
    animation = layers,     1, 2.6, water, slide
    animation = layersIn,   1, 2.4, cascade, slide
    animation = layersOut,  1, 1.9, stream, slide
    animation = workspaces, 1, 2.8, flow
    animation = specialWorkspace, 1, 2.5, water
    animation = border,     1, 2.9, water
    animation = borderangle,1, 3.5, flow
}

decoration {
    rounding = 15
    
    shadow {
        enabled = true
        range = 25
        render_power = 3
        color = $shadow_color
        color_inactive = $shadow_inactive
        offset = 0 5
        scale = 1.0
    }
    
    blur {
        enabled = true
        size = 4
        passes = 5
        contrast = 1.0
        brightness = 0.8
        vibrancy = 0.2
        noise = 0.01
        ignore_opacity = false 
        popups = true
        popups_ignorealpha = 0.5
    }
    
    active_opacity = 1.0
    inactive_opacity = 0.95
    fullscreen_opacity = 1.0 
}

misc {
    background_color = $background 
}

layerrule {
  name = layerrule-1
  blur = on
  ignore_alpha = 0
  match:namespace = walker
}


layerrule {
  name = layerrule-2
  blur = on
  ignore_alpha = 0.1
  match:namespace = waybar
}


layerrule {
  name = layerrule-3
  blur = on
  ignore_alpha = 0.1
  match:namespace = notifications
}


layerrule {
  name = layerrule-4
  blur = on
  ignore_alpha = 0.1
  match:namespace = swayosd
}

